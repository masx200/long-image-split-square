export declare function getBin(name: string): string;
export default function cropimagewrite(inputfile: string, outfile: string, width: number, height: number, left: number, top: number, maxpixels: number): Promise<void>;
